import { AdminUsers } from '@/components/admin/admin-users'

/**
 * Página de gestão de usuários do painel administrativo.
 *
 * Permite buscar, filtrar, visualizar e gerenciar
 * contas de usuários da plataforma Clausent.
 *
 * Rota: /[locale]/admin/users
 */
export default function AdminUsersPage() {
  return <AdminUsers />
}
